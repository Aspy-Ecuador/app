interface FormViewProps {
    isEdit: boolean;
    userId?: number;
    role: string;
    isRegister?: boolean;
}
export default function FormView({ isEdit, userId, role, isRegister, }: FormViewProps): import("react/jsx-runtime").JSX.Element;
export {};
