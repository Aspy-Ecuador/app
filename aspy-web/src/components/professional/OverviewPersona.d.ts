import { User } from "@/types/User";
interface OverviewPersonaProps {
    selectedData: User;
    moreInfo: () => void;
}
export default function Overview_persona({ selectedData, moreInfo, }: OverviewPersonaProps): import("react/jsx-runtime").JSX.Element;
export {};
